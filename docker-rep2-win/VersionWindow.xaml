<Window x:Class="docker_rep2_win.VersionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:docker_rep2_win"
        Title="バージョン情報" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow" Background="White"
        Loaded="Window_Loaded">
    <Grid Margin="20" KeyboardNavigation.DirectionalNavigation="Cycle" KeyboardNavigation.TabNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{x:Static local:AppInfo.AppFullName}" FontSize="20" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding PublisherInfo}" FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="{Binding VersionInfo}" FontSize="16" Margin="0,10"/>
            <TextBlock>
                <Hyperlink NavigateUri="{Binding AppGitHubUri}" RequestNavigate="Hyperlink_RequestNavigate">
                    <TextBlock Text="{x:Static local:AppInfo.AppGitHubUrl}"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <Button x:Name="BtnClose" Grid.Row="2" Style="{StaticResource PageButtonStyle}" Content="閉じる" HorizontalAlignment="Right" Width="100" Height="35" Click="Close_Click" IsDefault="True"/>
    </Grid>
</Window>
