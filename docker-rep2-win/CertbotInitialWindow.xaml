<Window x:Class="docker_rep2_win.CertbotInitialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="証明書の初回取得" Height="380" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="証明書取得パラメータの入力" FontSize="16" FontWeight="Bold"/>
            <TextBlock Text="初回の証明書取得に必要な情報を入力してください。" Foreground="DimGray" FontSize="11"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="取得ドメイン名 (例: example.com)" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtDomain" Height="25" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Text="連絡用メールアドレス (例: user@example.com)" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtEmail" Height="25" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Text="DNSプラグイン名 (例: dns-mydnsjp, dns-cloudflare)" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtPlugin" Height="25" VerticalContentAlignment="Center" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnOk" Style="{StaticResource PageButtonStyle}" Content="取得実行" Width="100" Height="35" Margin="0,0,10,0" Click="BtnOk_Click"/>
            <Button x:Name="BtnCancel" Style="{StaticResource PageButtonStyle}" Content="キャンセル" Width="100" Height="35" IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>